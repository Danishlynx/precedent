{
  "name": "precedent",
  "version": "1.0.0",
  "description": "Decision memory for Slack — watches channels, logs decisions with receipts, answers in the assistant panel, and exposes the log via MCP.",
  "private": true,
  "main": "src/app.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node src/app.js",
    "seed": "node scripts/seed.js",
    "mcp": "node mcp/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@slack/bolt": "^4.7.3",
    "@slack/web-api": "^7.0.0",
    "better-sqlite3": "^12.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "node-cron": "^4.6.0",
    "zod": "^3.25.0"
  }
}
